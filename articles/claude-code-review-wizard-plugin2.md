---
title: "AskUserQuestionをブラウザで：Claude Codeの質問をウィザードUIにするプラグイン review-wizard"
emoji: "🧙"
type: "tech"
topics: ["claudecode", "ai", "plugin", "nodejs"]
published: false
---

## TL;DR

- Claude Code の AskUserQuestion はターミナル UI なので、質問が多いときや、選択肢の説明を読み比べたいときは見づらい。
- review-wizard は、その質問をブラウザのウィザード画面（1問ずつ表示、ステッパー付き）で出し、回答を JSON で返すプラグイン。`--rich` を付けると表や図（SVG）も添えられる。
- 実装は一時的なローカル HTTP サーバ方式で、外部 npm 依存はゼロ。回答を受け取ったら閉じて終了し、常駐しない。
- リポジトリ: <https://github.com/uehaj/review-wizard>

## はじめに

NTTテクノクロスの上原です。

Claude Code には AskUserQuestion という仕組みがあります。作業の分岐点で選択肢を出し、ターミナルの中でそのまま答えられる。1〜2問ならこれで何の不満もありません。手を止めずに答えて、すぐ作業に戻れます。

ところが、質問が3問4問と続き、選択肢ごとに数行の説明が付きはじめると、様子が変わります。2問目を読んでいるうちに1問目の説明は画面の上へ流れていき、選択肢どうしを見比べようとすれば、スクロールで上下を往復することになる。読み直せないわけではありません。ただ、3問目に答えるころには、1問目で自分が何を選んだのかも怪しくなっています。

質問を短くしてもらえば済む話だろうか、とも考えました。しかし説明を削れば、判断の材料も一緒に消えます。答えづらさの正体は質問の量ではなく、「並べて見比べる」という操作が、縦一列に流れていくターミナルと相性が悪いことにありました。

であれば、見比べたい質問だけをブラウザに出せばいい。そう考えて作ったのが review-wizard というプラグインです。まずは動いている画面を見てください。

## 画面を見てみる

「認証方式の設計レビュー」を例にします。セッション管理方式の単一選択、ログイン手段の複数選択、有効期限の選択と自由記述、という3問構成です。

1問目です。上部のステッパーが現在地（認証方式）を示します。単一選択のラジオボタンに、選択肢ごとの補足説明。この質問では選択肢の上に、方式の比較表とトークンの流れを示す図（SVG）も出しています（後述の `--rich` によるものです）。ここでは「ハイブリッド」を選択済みです。下部には全問共通の「その他（自由記述）」欄があります。

![質問1/3の画面。上部に現在位置を示すステッパー（①「認証方式」を強調、②③④は未到達）、ヘッダーチップ「認証方式」、質問文の下に方式ごとの比較表とトークンの流れを示すインライン SVG 図、セッション管理方式を選ぶ単一選択のラジオボタンで「ハイブリッド」が選択済み、下部に「その他（自由記述）」欄と「戻る/次へ」ボタン](/images/review-wizard-q1.png)

2問目は複数選択です。チェックボックスで「Google OAuth」と「パスキー」を選んでいます。ステッパーの1問目は完了（チェック）に変わりました。

![質問2/3の画面。1問目が完了チェック、2問目「ログイン手段」が現在。ログイン手段を複数選択できるチェックボックスで「Google OAuth」と「パスキー」の2つが選択済み](/images/review-wizard-q2-multi.png)

3問目では、選択肢に加えて「その他」欄にも記入しています。選択と自由記述は同時に持たせられます。

![質問3/3の画面。有効期限の選択肢で「8時間」を選択し、その他欄に「モバイルアプリ向けには30日のリフレッシュトークンを別途検討したい」と入力済み](/images/review-wizard-q3-other.png)

最後まで答えると確認画面です。全問の回答が1画面に並び、各行の「修正」から該当の質問に戻ってやり直せます。ターミナルではスクロールの往復だった「見比べ」が、ここでは一覧で済みます。

![確認画面。ステッパーは全ステップ完了。全3問の質問文と回答のサマリが行ごとに並び、各行に「修正」リンク、下部に「戻る」ボタンと「回答を送信」ボタン](/images/review-wizard-summary.png)

「回答を送信」を押すと、サーバが回答 JSON を書き出して閉じ、ブラウザには完了画面が残ります。

![送信完了画面。「回答を送信しました」という見出しと「このタブは閉じて構いません。」という案内](/images/review-wizard-done.png)

## 裏のしくみ

ブラウザに画面を出すといっても、フレームワークもビルドも使っていません。中身は `scripts/review_wizard.ts` という単一ファイルの CLI で、しくみは3点だけです。

まず、起動すると 127.0.0.1 に一時的な HTTP サーバを立てます。URL には毎回ランダムなワンタイムトークンを埋め込み、そのパスでだけウィザード画面を返します。ブラウザが URL を開き、回答が送信されると、サーバは回答を JSON で書き出してそのまま閉じます。起動して、答えを待って、閉じる。ポーリングも常駐もない短命なプロセスです。

次に、外部 npm 依存がありません。使うのは Node 標準モジュールだけで、画面の HTML と CSS と JavaScript もすべてスクリプト内にインラインで持っています。外部リソースへの参照が一切ないので、オフラインでも動きます。

最後に、`review_wizard.ts` を `.ts` のまま `node` で直接実行します。Node の Type Stripping（型注釈をコンパイルなしで取り除く機能）を使うためで、Node 23.6 以降が前提です。実行時に `ExperimentalWarning` が出ますが、Type Stripping が実験的機能である旨の通知で、動作には影響しません。

では、答え忘れた項目があったらどうなるのでしょうか。選択肢か「その他」のどちらかが埋まらないかぎり「次へ」は押せません。受け取るサーバ側でも同じ条件を検証し、満たさなければ 400 を返します。画面とサーバの二重チェックで、不完全な回答が JSON に書き出されることを防いでいます。

質問 JSON と回答 JSON の形式は、外部利用者との互換性契約として今後も変えない方針です（`detail` のような任意項目の追加はしても、既存の形式は壊しません）。項目の詳細は README にあります。

## 導入と使い方

マーケットプレイス経由なら次の2行です。

```bash
claude plugin marketplace add https://github.com/uehaj/review-wizard.git
claude plugin install review-wizard@review-wizard-marketplace
```

開発中や導入前の確認では、ローカルのディレクトリを直接読み込ませます。

```bash
claude --plugin-dir /path/to/review-wizard
```

Claude Code から使うときは、質問 JSON を一時ファイルに書き、バックグラウンドで実行します。回答が送信されるまでプロセスは終わらないので、フォアグラウンドで待つと Claude Code 自体が止まってしまいます。

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/review_wizard.ts" \
  --questions <in.json> --out <out.json> --timeout 1800
```

起動すると標準出力に `review_wizard: <URL>` が出て、その URL が自動で開きます。プロセスが終わったら、終了コードで結果を判別し、`--out` の回答 JSON を読みます。終了コードは、回答受領が 0、入力エラーが 1、タイムアウトやサーバ起動失敗が 2、中断が 130 です。

### 図や表で補足する（--rich）

一行の補足説明で選べる質問ばかりではありません。さきほどの1問目のセッション管理方式などは、方式ごとの性質を表で見比べ、トークンの流れを図で確かめて、はじめて選べる類の質問です。そのために、各質問へ `detail` という項目を付けられるようにしました。`detail` は質問文の下に描画され、次のものが使えます。

- 表：`<table>`
- 図：インライン `<svg>`（テキストなので外部依存もオフライン制約もそのまま）
- 画像やスクショ：`<img src="data:image/png;base64,...">`（データ URI で埋め込み、外部 URL は使わない）

質問 JSON にはこう書きます。

```json
{
  "question": "セッション管理方式はどれにしますか？",
  "header": "認証方式",
  "detail": "<table>…</table><svg>…</svg>",
  "options": [{ "label": "サーバセッション" }, { "label": "JWT" }]
}
```

この機能は既定ではオフで、CLI に `--rich` を付けたときだけ有効になります。`--rich` が無ければ、サーバは `detail` をクライアントへ渡す前に取り除くので、HTML の注入自体が起きません。`detail` は作者（利用者自身かエージェント）が書く信頼済みの HTML としてそのまま描画するため、127.0.0.1 とワンタイムトークンで自分だけが見るローカル用途に限る、という前提で割り切っています。

### 使いどころ

使い分けは SKILL.md にも書いてあります。質問が3問以上、あるいは選択肢の比較検討が要るときは review-wizard、1〜2問の即答はターミナル内の AskUserQuestion のまま、という方針です。

なお、編集やコマンド実行を自動承認するモードで長時間走らせる使い方だと、そもそも途中で質問される機会自体が減ります。review-wizard が効くのは、判断を質問で拾いながら進める対話的な使い方をしているときです。

## まとめ

スクロールを上下しながら選択肢を見比べていた冒頭の場面は、ブラウザの1画面に収まるようになりました。仕掛けは一時ローカルサーバと外部依存ゼロの単一スクリプトで、質問と回答を JSON でやり取りするだけです。表や図が判断に効く質問には `--rich` があります。

一方で、1〜2問の即答なら、手を止めずに答えられる AskUserQuestion のほうが速いままです。どの質問からを review-wizard に任せるかは、ふだんの作業でどんな質問に答えづらさを感じているかを手がかりに、利用者が自分で決めて設定するものです。実際に試して、自分に合う線を見つけてみてください。

## 参考

- [Claude Code Plugins（公式ドキュメント）](https://code.claude.com/docs/en/plugins)
- リポジトリ: <https://github.com/uehaj/review-wizard>
- この記事のソース: <https://github.com/uehaj/zenn-articles>
